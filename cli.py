# cd => current directory
# cd <Klasör adı> => sizi o dizine götürür. Örn cd Desktop
# mkdir (windows/mac) klasör oluşturur. örn mkdir "Pyt4181"
# md (Windows) klasör oluşturur. örnek "md Pyt4181"
# cd .. bir üst dizine çıkar (geri)
# (Windows) echo > cli.py       =>  bulunduğu dizin içerisinde cli.py adında bir belge oluşturur.
# (Mac) touch cli.py            =>  bulunduğu dizin içerisinde cli.py adında bir belge oluşturur.

# dir (Windows) ls (Mac) dizindeki dosyaları listeler.
# rm belgeadı                   => adını verdiğiniz belgeyi siler.
# rmdir dosyaadı                => adını verdiğiniz dosyayı siler.

# tekli yorum satırı (#) bu alana yazılanlar kodlar derlenmez.